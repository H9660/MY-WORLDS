#include <iostream>
using namespace std;

int main() {
	int r;
	cin>>r;
	while(r--)
	{
	    int l;
	    char prev;
	    cin>>l;
	    char*arr=new char[l]
	    int i=0;
	    while(i<l)
	    {
	        cin>>arr[i];
	        i++;
	    }
	    
	    if(arr[n-1]!='T')
	   { cout<<"Invalid"<<endl;
	     continue;
	   }
	   
	    i=0;
	    int j;
	    int prev;
	    
	   while(i<n)
	   {j=i;
	   prev=arr[i];
	   while(j<n)
	       if(arr[i]=='H')
	       prev=arr[i];
	       
	       if(arr[i])
	   }
	        
	}
	
	return 0;
}
        