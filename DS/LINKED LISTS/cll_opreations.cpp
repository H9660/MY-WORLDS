#include<iostream>
#include<string.h>
#include "ALL_LL.h"
using namespace std;


int main()
{ system("cls");
    cslnode*tail=NULL;
    // tail=createlist(tail);
  tail=cslcreatelist(tail);  // print(tail);
    cslprint(tail);
  
return 0;
}